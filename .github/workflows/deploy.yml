name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render Deployment
        run: curl -X POST "https://api.render.com/deploy/srv-cul5ucrqf0us73bv6vc0?key=GWQhEqxQh_s"
